

## Project setup
- python version = 3.12

Run `docker compose up db -d  && docker compose up -d && docker exec django-crud-web-1 python manage.py migrate`

Access `http://127.0.0.1:8000/` to test your environment


## Tests

