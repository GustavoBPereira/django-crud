version: "3.9"

services:
  app:
    build:
      context: .
    restart: always

  proxy:
    build:
      context: ./proxy
    restart: always
    depends_on:
      - app
    ports:
      - 80:8000
